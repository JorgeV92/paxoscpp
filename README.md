# paxoscpp

A compact educational Paxos repository in modern C++.

This repo keeps the implementation intentionally small:
- one C++ source file: `src/paxos_demo.cc`
- one CMake build: `CMakeLists.txt`

The goal is to make Paxos readable and runnable